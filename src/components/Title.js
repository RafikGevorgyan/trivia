import React from 'react';

function Title({ children }) {
    return <p className='font-bold text-[50px] text-green mt-[134px] leading-[75px] cursor-default'>{children}</p>;
}

export default Title;
